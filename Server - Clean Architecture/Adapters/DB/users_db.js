export const usersDb = Object.freeze({
    insert: async (user) => {
        // Insert user
    },
    deleteById: async (userId) => {
        // Deletes user by id
    },
    findById: async (userId) => {
        // returns user by id
    },
})